What is Secret?
	Secrets are very similar to ConfigMap, but they are used to store sensitive data such as passwords, API keys, SSH keys, OAuth tokens, Certificates;
	Note that secrets are also stored in etcd(like other objects). So, ensure that they are protected with encryption-at-rest and RBAC;
	Alternatively, use Vault to store and manage sesitive data;

Types of Secrets 
	generic  			-> general purpose secret; For eg, db username/password;
	docker-registry 	-> It stores credentials of a private docker registry; It is used by kubelet to pull images from private docker registries;
	tls 				-> It stores ssl keys and certs; This is useful for securing the communication b/w services, as well as services and external clients;
	ssh 				-> It stores ssh private keys; It is used to authenticate with other servers and services;

Pods can consume Secrets via the following methods:
	- As command line arguments to a container; // Avoid this, as it exposes secrets in process list(ps insidecontainer), pod spec(kubctl describe pod);
	- As environment variables in the container;
	- As a file in the volume mounted;



------------------------------------------------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------------------------------------------------
Note: We have already used Secrets without even knowing. Every Pod we created so far had a Secret mounted automatically by the system.


Best Practices:
	Implement proper RBAC controls
	Rotate secrets regularly
	Use encryption at rest
	Enable audit logging
	Follow least privilege access
	Integrate with identity providers
	Never store secrets in plain text
	Keep secrets isolated by environment

Enable encryption at rest (usually enabled by default with KMS).
✅ Use IAM or RBAC to strictly control access.
✅ Enable audit logging to track access to secrets.
✅ Avoid hardcoding secrets in code or committing them to version control.
✅ Use environment variables or volume mounts to inject secrets into apps securely.
✅ Rotate secrets regularly — most services support automatic rotation.
✅ Use Kubernetes external secret operators (like External Secrets Operator) to sync secrets from cloud managers into your clusters.

You can use Secrets by mounting them as files or injecting them as environment variables into your pods. 

K8s Secrets are just base64 encoded and stored in etcd by default. 

Supports RBAC and access control to limit who can access secrets.

Consider external secret stores for highly sensitive data.


	but data is base64 encoded before storing in etcd;
	Note that data is not encrypted, it is just encoded; Secrets are still .

	
	To improve security, EncryptionConfig feature in k8s can be used to encrypt sensitive data;

args: ["echo username: $(cat /etc/secret/username), password: $(cat /etc/secret/password) "]
------------------------------------------------------------------------------------------------------------------------------------------------------------
# Create 
kubectl create secret <type> <name> 
kubectl create secret generic <name> --from-literal=<key>=<value>
kubectl create secret generic <name> --from-file=<path_to_file>
kubectl create secret generic <name> --from-literal=url=https://my-secret-url-location.topsecret.com
kubectl create -f <resource-file>
kubectl create secret docker-registry <name> --docker-server=my.registry.com:5000 --docker-username=<username> --docker-password=<password> --docker-email=<email>
kubectl create secret tls <name> --cert=<path-to-cert-file> --key=<path-to-key-file>

# Get
kubectl get secrets																# List all secrets;



 -n <namespace>												# List all sectets in given namespace;
kubectl describe secret <name> 													# It prints all info about secret;
kubectl get secret <name> -o jsonpath='{.data}'									# It shows encoded key-value pairs stored in the data section;
kubectl get secret <name> -o jsonpath='{.data.password}' 						# It returns encoded password;
kubectl get secret <name> -o jsonpath='{.data.password}' | base64 --decode 		# It returns decrypted password;

kubect edit secret <name> 														# Edit secret;
kubectl delete secret <name> 													# Delete secret;

# Print decoded data in a secret with one command;
kubectl get secret <name> -o go-template='{{range $k,$v := .data}}{{"### "}}{{$k}}{{"\n"}}{{$v|base64decode}}{{"\n\n"}}{{end}}'

kubectl get secret my-secret -o jsonpath="{.data.password}" | base64 --decode



kubectl create secret generic my-secret \
  --from-literal=username=admin \
  --from-literal=password='s3cr3t'

apiVersion: v1
kind: Pod
metadata:
  name: mypod
spec:
  containers:
  - name: mycontainer
    image: nginx
    env:
    - name: USERNAME
      valueFrom:
        secretKeyRef:
          name: my-secret
          key: username
This sets the USERNAME environment variable inside the container using the secret value.



By default, Kubernetes secrets are encrypted using the AES-256 encryption algorithm, and access to the secrets is controlled by Kubernetes RBAC (Role-Based Access Control) rules.

A common practice is not to provide edit or delete access to someone who does not need them.

However, it’s important to note that Kubernetes secrets are not foolproof, and there are potential security risks that can compromise the security of your secrets.

For example, if an attacker gains access to the Kubernetes cluster or to a pod running an application that uses secrets, they may be able to access the secrets.

EncryptionConfig
	EncryptionConfig allows you to encrypt sensitive data in Kubernetes using a data encryption key. 
	When you create a Secret, Kubernetes will automatically encrypt the data using the encryption key, and store it in etcd in an encrypted format.

Note: The only significant difference between ConfigMaps and Secrets is that the latter creates files in tmpfs (temporary file storage).

 We’d need to combine Secrets with “Authorization Policies” to make the passwords, keys, tokens, and other types of data secure that should not be accessible publicly.

Secrets can be consumed by Pods as environment variables, command-line arguments, or as configuration files in a volume.
	Secret is , but it is 
	Secrets are base64-encoded by k8s before storing them in tmpfs on a Node(not on disk), so they are not wildly secure;
	Make sure you have appropriate role-based access controls (RBAC) to protect access to Secrets;
	Secrets are namespace aware;
	They can be accessed by Pods as environment variables, configuration files, or command-line arguments.
	 Though, it’s important to note that Secret data is not encrypted. 
	

kubectl -n test create secret docker-registry mc-registry --docker-server=esd-netops-docker-virtual.artifactory-lvn.broadcom.net --docker-username=YOUR_OKTA_ID --docker-password=YOUR_ARTIFACTORY_API_KEY_OR_OKTA_PASSWORD --docker-email=YOUR_EMAIL

Maven - https://www.jfrog.com/confluence/display/JFROG/Maven+Repository
Docker - https://www.jfrog.com/confluence/display/JFROG/Docker+Registry
Helm - https://www.jfrog.com/confluence/display/JFROG/Helm+Chart+Repositories
NPM - https://www.jfrog.com/confluence/display/JFROG/npm+Registry

Broadcom Customer Support Portal (CSP), support.broadcom.com, is the main portal for customers to download products and updates. Some products require registry type packages such as Docker, NPM, etc. which CSP does not support natively. There is a JFrog SaaS instance (packages.broadcom.com) which is used to host such artifacts and can become available via CSP to the entitled Broadcom's customers.

kubectl -n flow create secret docker-registry esd-netops-docker-virtual --docker-server=esd-netops-docker-virtual.artifactory-lvn.broadcom.net --docker-username=YOUR_OKTA_ID --docker-password=YOUR_ARTIFACTORY_API_KEY_OR_OKTA_PASSWORD --docker-email=YOUR_EMAIL



Built-in secrets 


generic 	-> 


generic Secrets can use --from-env-file, --from-file, and --from-literal as sources. They can be mounted as files or transformed into environment variables.




kubectl get secrets -n <namespace> 					# List all secrets;
kubectl describe secret/<name> -n <namespace> 		# Get more information about secret;


kubectl delete secret access-token
secret "access-token" deleted

If a Secret is deleted when a Secret volume is attached, it'll show an error message whenever the volume reference disappears:

Every Pod we created so far had a Secret mounted automatically by the system.

encode: echo -n "data" | base64						# -n : no trailing newline
decode: echo -n "encoded-data" | base64 -d  
-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------



-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

If the files /etc/secrets/jenkins-user and /etc/secrets/jenkins-pass are present, the init script will read them and use the content inside those files to define the username and the password.


The default-token-l9fhk Secret is created automatically by Kubernetes. It contains credentials that can be used to access the API.
Unless we tweak Service Accounts, every Pod we create will have this Secret. 
Let’s confirm if that is indeed true:
kubectl describe pods
We can see that it references the auto-generated Secret default-token-l9fhk and mounts it as /var/run/secrets/kubernetes.io/serviceaccount. 

https://kubernetes.io/docs/concepts/configuration/secret/#secret-types
https://kubernetes.io/docs/reference/kubectl/generated/kubectl_create/kubectl_create_secret_docker-registry/
https://kubernetes.io/docs/concepts/configuration/secret/

There are dedicated secret types for common data structures, such as Docker config files, SSH keys, TLS certificates, and Kubernetes service account tokens.

Opaque	arbitrary user-defined data
kubernetes.io/service-account-token				Kubernetes auth token - ServiceAccount token
kubernetes.io/dockercfg							Docker registry auth - serialized ~/.dockercfg file
kubernetes.io/dockerconfigjson					Latest Docker registry auth - serialized ~/.docker/config.json file
kubernetes.io/basic-auth	credentials for basic authentication
kubernetes.io/ssh-auth	credentials for SSH authentication
kubernetes.io/tls	data for a TLS client or server
bootstrap.kubernetes.io/token	bootstrap token data



kustomize
	Generators such as Kustomize help quickly generate secrets.
	



4.3. Restrict Access to Secrets
It’s important to restrict access to secrets to only the applications and services that need them. This helps to prevent unauthorized access to sensitive data. In Kubernetes, we can use RBAC (Role-Based Access Control) to restrict secret access.

4.4. Rotate Secrets Regularly
Yet another way to protect secrets is to rotate their contents regularly. This helps to prevent attacks that rely on the fact that the secret has been in use for a long time. We can use tools like kubctl-secrets-controller to automate the rotation of secrets.

4.5. Use Encryption at Rest and in Transit
It’s important to use encryption to protect secrets at rest (when stored) and in transit (when transmitted between services). In Kubernetes, we can use TLS (Transport Layer Security) to encrypt communication between services and encryption providers like sops or vault to encrypt secrets at rest.


https://phoenixnap.com/kb/kubernetes-secrets
https://www.loft.sh/blog/how-to-create-and-manage-kubernetes-secrets-a-tutorial
https://web.archive.org/web/20211122093817/https://dchua.com/posts/2017-04-21-load-env-variables-from-configmaps-and-secrets-upon-pod-boot/
https://phoenixnap.com/kb/kubernetes-secrets
https://medium.com/@subhampradhan966/kubernetes-secrets-how-to-create-use-and-manage-secrets-in-kubernetes-a23663a81d26 - TLS secret
https://medium.com/@bm54cloud/kubernetes-secrets-77a798f412aa


Insecure configurations
Almost everything Kubernetes needs is stored in etcd. This includes Secrets. The problem is that they are stored as plain text. Anyone with access to etcd has access to Kubernetes Secrets. We can limit access to etcd, but that’s not the end of our troubles.

etcd stores data to disk as plain text. Restricting the access to etcd still leaves the Secrets vulnerable to anyone who has access to the file system. That, in a way, diminishes the advantage of storing Secrets in containers in tmpfs. There aren’t manybenefits of having them in tmpfs used by containers if those same Secrets are stored on disk by etcd.

Even after securing access to etcd and making sure that unauthorized users do not have access to the file system partition used by etcd, we are still at risk. When multiple replicas of etcd are running, data is synchronized between them. By default, etcd communication between replicas is not secure. Anyone sniffing that communication could get a hold of our Secrets.

Kubernetes Secrets are a step in the right direction. It is, without a doubt, better to use Secrets than to expose confidential information as environment variables or other less secure methods. Still, Secrets can give us a false sense of security.

The way to secure Secrets
We need to take additional precautions to protect ourselves. This includes, but is not limited to, the following actions:

Secure the communication between etcd instances with SSL/TLS.

Limit access to etcd and wipe the disk or partitions that were used by it.

Do not define Secrets in YAML files stored in a repository. Create Secrets through ad-hoc kubectl create secret commands. If possible, delete commands history afterward.

Make sure that the applications using Secrets do not accidentally output them to logs or transmit them to other applications.

Create policies that allow only trusted users to retrieve Secrets. However, you should be aware that even with proper policies in place, any user with permissions to run a Pod could mount a Secret and read it.

We did not yet explored etcd configuration, nor did we learn how to set up authorization policies. For now, just remember that Secrets are not as secure as we might think, at least not the ones provided by the Kubernetes community. We do, however, encourage you to use them as long as you’re aware of their shortcomings.

Kubernetes stores Secrets in etcd. By default, they are exposed, and we need to take extra precautions to protect them.

Secrets create files in a tmpfs.

) Secrets are stored in etcd as  plain text.


echo -n 'mcheedhe' | base64
echo -n 'Welcome123' | base64


# It is your responsibility to base-64 encode text before storing in data;
# Otherwise, you will get this error: "invalid base-64 data" 
# echo -n 'mcheedhe' | base64		-> bWNoZWVkaGU=
# echo -n 'Welcome123' | base64 	-> V2VsY29tZTEyMw==
apiVersion: v1
kind: Secret
metadata:
  name: s1
type: Opaque
data:
  username: bWNoZWVkaGU=			# base64 for mcheedhe
  password: bXktZGItcGFzc3dvcmQ= 	# base64 for Welcome123 

# Use stringData field, if you want to store plain text directly in secret;
# K8s takes stringData and encodes it using base64 and creates data field internally;
apiVersion: v1
kind: Secret
metadata:
  name: s2
type: Opaque
stringData:
  username: mcheedhe
  password: Welcome123


-------------------------
If you pulling docker image from a private registry:
kubectl create secret docker-registry my-registry-secret \
  --docker-server=REGISTRY_URL \
  --docker-username=USERNAME \
  --docker-password=PASSWORD \
  --docker-email=EMAIL

Then attach it to your Pod or ServiceAccount.
-------------------------
