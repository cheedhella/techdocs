apiVersion: v1
kind: Service
metadata:
  name: {{ include "user-crud-app.fullname" . }}
  labels:
    {{- include "user-crud-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "user-crud-app.selectorLabels" . | nindent 4 }}
