Logic operators: and, or, not;
Comparision operators: eq, ne, lt, le, gt, ge;
Loop control: if/else/end, if/else if/else/end, with, range;
with, range;




eq arg1 arg2 -> Returns true if they are equal; otherwise, returns false;
if else -> 
if else/if else -> 

with to specify a scope
range, which provides a "for each"-style loop



values.yaml 
myapp:
    env: prod 
    enableFeature: true

spec:
{{- if eq .Values.myapp.env "prod" }}
  replicas: 4
{{- else if eq .Values.myapp.env "qa" }}
  replicas: 2
{{- else }}
  replicas: 1
{{- end }}


{{- if and .Values.myapp.enableFeature (eq .Values.myapp.env "prod") }}
  replicas: 2
{{- end }}


{{ with PIPELINE }}
  # restricted scope
{{ end }}


with 
    - It alllows you to set the current scope(.) to a particular object;
    - After setting the scope to an object, in case if you want to access the root object inside with block, prepend the root with $;
    - 
 The range function will "range over" (iterate through) the pizzaToppings List



 Variables 
    - variables are used to simplify code;
    - Primary used in WITH, RANGE functions and named templates;
    - variables are assigned with special assignment operator :=

    How to define a variable?
        {{- $chartName := .Chart.Name -}}
    \How to refer a varialbe?
        appName: {{ $chartName }}

    - during declaration you can also use pipeline and functions;
    {{- $chartName := .Chart.Name | quote | upper -}}


helm template myapp1 .
